# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-12 | architecture | State management library | Zustand with persist middleware | Simple API, minimal boilerplate, persist middleware handles localStorage for API keys and theme. 6 stores: filter, results, scan, apiKey, theme, chain. | no |
| 2 | 2026-03-12 | architecture | Server state management | TanStack Query v5 | useMutation for scan flow (progress/cancel), useQuery for option chains. Built-in caching, retry, loading states. | no |
| 3 | 2026-03-12 | architecture | Component library | shadcn/ui with Tailwind v4 | Copy-paste components, full control over styling, CSS variables theming, Radix primitives for accessibility. | no |
| 4 | 2026-03-12 | architecture | Animation library | Framer Motion | AnimatePresence for modal transitions, spring physics for toggles, stagger for page load. | no |
| 5 | 2026-03-12 | scope | Score tooltip style | Numeric breakdown tables, not radar charts | Radar charts are harder to read at tooltip size. Numeric breakdown with score bars is clearer and simpler to implement. | yes |
| 6 | 2026-03-12 | scope | Options data providers | Finnhub + Alpaca + Massive.com (Polygon) | User wants all three. Massive.com free tier has 5 calls/min limit — needs careful rate limiting. | no |
| 7 | 2026-03-12 | quality | Testing strategy | Vitest unit tests for scoring/filtering parity | Critical business logic (scoring weights, filter logic, formatters) must produce identical outputs to vanilla app. Tests catch regressions during migration. | no |
| 8 | 2026-03-12 | architecture | Font hosting | CDN (Google Fonts + Fontshare) | Faster to ship. Self-host later if needed for performance/offline. | yes |
| 9 | 2026-03-12 | deployment | Deploy target | Static SPA (Vite build) | Build outputs static files deployable to Vercel/Netlify/GitHub Pages. No SSR needed. | no |
| 10 | 2026-03-12 | architecture | Migration approach | Ground-up rewrite, not incremental | Vanilla monolith has no module boundaries. Cleaner to extract business logic into typed modules and build new component tree than to incrementally migrate DOM manipulation code. | no |
| 11 | 2026-03-12 | toolchain | Vite + React plugin versions | vite@7.3.1 + @vitejs/plugin-react@5.2.0 | @tailwindcss/vite@4 requires vite 5-7, @vitejs/plugin-react@6 requires vite 8. Using plugin-react@5.2.0 (supports vite 4-8) resolves the conflict. Revisit when @tailwindcss/vite adds vite 8 support. | yes |
| 12 | 2026-03-12 | theming | shadcn/ui CSS variable pattern | theme.css (oklch vars) → index.css (@import chain + @theme inline) | Follows shadcn/ui v4 manual install pattern. Theme vars separated into theme.css for easy palette swapping. All components consume via Tailwind utilities (text-primary, bg-background, etc.). | no |
| 13 | 2026-03-12 | toolchain | Path alias resolution | vite-tsconfig-paths plugin | Reads @/ alias from tsconfig.json, works in both Vite and tsc without duplicating config in resolve.alias. | no |
| 14 | 2026-03-12 | migration | Vanilla index.html preserved | Renamed to index.vanilla.html | Preserves original for reference during extraction tasks. Will be deleted in S08 cleanup. | no |
| 15 | 2026-03-12 | architecture | escapeHtml implementation | String-based replace chain (not DOM) | Must run in Node test environment (Vitest). Produces identical output for all HTML entity cases. | no |
| 16 | 2026-03-12 | architecture | Scoring function purity | New objects returned, no mutation, explicit parameters | computeWheelMetrics takes earningsEntry as param instead of global lookup. All scoring functions return spread copies. Required for React state management and testability. | no |
| 17 | 2026-03-12 | architecture | NaN-sentinel field typing | undefined instead of NaN + isNaN() | TypeScript-idiomatic. FilterState uses `number \| undefined` for optional numeric filters (maxDebtEquity, minNetMargin, etc.). Filter functions guard with `!== undefined`. | no |
| 18 | 2026-03-12 | pattern | scanStore state machine | Explicit phase enum (idle\|running\|complete\|error) | Clearer than inferring state from boolean flags (running + error). Single source of truth for scan lifecycle. All UI can switch on phase. | no |
| 19 | 2026-03-12 | pattern | apiKeyStore derived status | Computed via deriveStatus() on every mutation + rehydrate, excluded from persistence via partialize | Status is purely derived from key presence. Never stale, never serialized. Pattern avoids persist/runtime state drift. | no |
| 20 | 2026-03-12 | pattern | Rate limiter: setInterval + FIFO queue | TokenBucketRateLimiter uses setInterval for refill + queue.shift() for FIFO drain | Simpler than setTimeout chains. Handles concurrent waiters correctly — each refill cycle drains as many waiters as tokens available. Must call dispose() to prevent leaked intervals. | no |
| 21 | 2026-03-12 | pattern | Service client error convention | All services throw ApiError(message, status, endpoint, responseBody) | Downstream catches `instanceof ApiError` for typed handling. Status codes: 429=rate limited, 401=bad key, 403=forbidden. endpoint never includes API key for safety. | no |
| 22 | 2026-03-12 | pattern | Finnhub 429 retry strategy | 3 attempts, multiplicative 1200ms backoff, re-acquire rate limiter token after backoff | Prevents post-retry bursts from immediately hitting the rate limit again. Other services (Alpaca, Massive) don't retry — callers handle errors. | no |
| 23 | 2026-03-12 | toolchain | shadcn CLI component generation | Write components manually instead of using `npx shadcn add` | CLI creates files at wrong path (literal `./@/` dir) and imports from `radix-ui` meta-package instead of scoped `@radix-ui/*`. Manual write ensures correct paths and imports. | yes |
| 24 | 2026-03-12 | pattern | Mobile sidebar overlay mounting | Conditional DOM mount (sidebarOpen \|\| isClosing) + onTransitionEnd unmount | CSS-only pointer-events-none on fixed inset-0 overlay still intercepted browser hit tests. Conditional mounting avoids the issue and keeps DOM clean when sidebar is closed. isClosing flag preserves slide-out animation before unmount. | no |
| 25 | 2026-03-12 | pattern | Header ownership | DashboardLayout renders Header internally | DashboardLayout owns sidebarOpen state and passes toggle callback to Header directly. Avoids prop-drilling toggle through App.tsx. Header API is onMenuClick callback, not a ReactNode slot. | no |
| 26 | 2026-03-12 | pattern | Preset detection strategy | Derived via useMemo, not tracked as local state | Compares full filter store state against PRESETS record. Eliminates sync bugs between preset dropdown and manual edits. If any field differs from all presets, shows "Custom". | no |
| 27 | 2026-03-12 | pattern | Weight redistribution algorithm | Proportional distribution with fractional-sort remainder | redistributeWeights exported as pure function. Distributes difference proportionally by share of remaining total, floors values, assigns rounding remainder to weight with largest fractional part. Guarantees integer sum=100. | no |
| 28 | 2026-03-12 | pattern | NumberInput draft state | Internal string state commits on blur/Enter | Prevents cursor jumping during editing. Store receives parsed number only on commit, not on every keystroke. Handles undefined ↔ empty string conversion for optional numeric fields. | no |
| 29 | 2026-03-15 | pattern | useShallow for multi-field Zustand selectors | Wrap object-returning selectors with useShallow from zustand/react/shallow | Zustand selectors returning new object literals ({a: s.a, b: s.b}) create new references every render, triggering React's "getSnapshot should be cached" infinite loop in StrictMode. useShallow does shallow comparison to prevent unnecessary re-renders. Apply to all selectors that return derived objects. | no |
| 30 | 2026-03-15 | architecture | Scan orchestrator decoupled from React | runScan() in src/lib/scan.ts has zero store imports — all side effects via callbacks (onTick, onCandidateFound, onPhaseChange) | Keeps scan logic testable and reusable. React bridging happens in useScanRunner hook via useMutation. Auth errors (401/403) thrown immediately rather than swallowed per-ticker. | no |
| 31 | 2026-03-15 | pattern | CSV export testability split | buildCSVContent() pure function + exportCSV() DOM side-effect wrapper | Pure content builder is testable in Vitest jsdom. DOM-dependent Blob+anchor download isolated in exportCSV(). Prevents test environment issues with URL.createObjectURL. | no |
| 32 | 2026-03-15 | architecture | Chain fetcher decoupled from React | fetchChain() in src/lib/chain.ts has zero store imports — provider path dispatching via params, not store reads | Follows scan.ts Decision #30 pattern. Makes chain merge logic testable with mocked services. React bridging via useChainQuery hook. | no |
| 33 | 2026-03-15 | pattern | Chain modal open state | Explicit isOpen + symbol in chainStore, not derived from chainData !== null | Allows modal to show loading state before data arrives. open(symbol) clears prior data and sets loading intent. close() resets everything. | no |
| 34 | 2026-03-15 | pattern | DEV-only window store exposure | `window.__chainStore` etc. in App.tsx, guarded by `import.meta.env.DEV` | Enables browser console testing of store state. Tree-shaken in production build. Separate HMR module instances — doesn't trigger React re-renders, but useful for state inspection. | yes |
| 35 | 2026-03-15 | pattern | Radix Dialog + motion integration | forceMount on Overlay/Content + AnimatePresence + open prop from consumer | Standard Radix `data-[state]` animations can't do exit transitions without forceMount. DialogContent accepts `open` prop, wraps overlay and content each in their own AnimatePresence with motion.div. Spring slide-up on content (stiffness: 300, damping: 30), simple fade on overlay. | no |
| 36 | 2026-03-16 | pattern | Tooltip animation without tw-animate-css | Pure CSS transitions via opacity/scale + data-[state] selectors | tw-animate-css removed as dependency. Tooltip uses `transition-[opacity,transform] duration-200` with `data-[state=open]:opacity-100 data-[state=open]:scale-100` instead of keyframe-based `animate-in fade-in-0 zoom-in-95`. Simpler, zero-dep approach. slide-in-from-* directional classes dropped as unnecessary for tooltip UX. | no |
